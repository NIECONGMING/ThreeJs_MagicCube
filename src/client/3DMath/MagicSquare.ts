// import { OrbitControls } from "../../lib/three/OrbitControls";

export class  MagicSquare
{
    // public initCamera(){
    //     _camera = new THREE.PerspectiveCamera(45,_width/_height , 1 ,1000);
    //     _camera.position.set(200,400,600);
    //     _camera.up.set(0,1,0);  //Y轴正方向
    //     _camera.lookAt(_origPoint); 
        
    //     //视角控制
    //     controller = new OrbitControls(_camera, _renderer.domElement);
    //     controller.target = origPoint;//设置控制点
    // }
}